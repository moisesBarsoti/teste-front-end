import TopBar from '../TopBar';
import './index.scss';

export default function Header() {
    return (
        <header aria-label="Cabeçalho da página">
            <TopBar />
        </header>
    )
}